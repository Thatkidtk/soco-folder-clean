[project]
name = "file-cleanup"
version = "1.0.0"
description = "Clinic-safe SOCOM folder cleanup tool with CLI and GUI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
soco-clean = "main:main"
